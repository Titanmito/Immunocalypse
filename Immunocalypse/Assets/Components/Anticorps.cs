using UnityEngine;

public class Anticorps : MonoBehaviour {
	// This component marks the anticorps entity and keeps its type (1 or 2) so we can find the allies we want.
	// There are two types of anticorps:
	// type 1 -> attacks virus
	// type 2 -> attacks bacterie
	public int type;
}